name: firebase_template
description: A portfolio of work. By Patrick Kelly
 
publish_to: 'none'  
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.14.4
  cupertino_icons: ^1.0.0
  firebase_analytics: ^6.3.0
  firebase_auth: ^0.18.4+1
  firebase_core: ^0.5.3
  firebase_messaging: ^7.0.3
  font_awesome_flutter: ^8.11.0
  http: ^0.12.2
  provider: ^4.3.2+3
  rxdart: ^0.25.0
  google_fonts: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

 
flutter:

 
  uses-material-design: true
  assets:
    - assets/
